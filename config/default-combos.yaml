# Default Combos - v1.0.0
# All skill IDs verified against scan results (103 skills)

combos:
  - name: frontend-dev
    description: "Frontend design to implementation pipeline"
    type: chain
    execution: serial
    skills:
      - frontend-design
      - ts-react-nextjs

  - name: frontend-vue
    description: "Vue frontend pipeline: UI/UX design followed by TypeScript and Vue/Svelte implementation"
    type: chain
    execution: serial
    skills:
      - frontend-design
      - ts-vue-svelte

  - name: api-first
    description: "API design to implementation: RESTful API design, Python patterns, and testing strategies"
    type: chain
    execution: serial
    skills:
      - api-rest-design
      - python-patterns
      - testing-strategies

  - name: deploy-pipeline
    description: "Full deployment pipeline: testing strategies, containerization with Docker, and Kubernetes deployment"
    type: chain
    execution: serial
    skills:
      - testing-strategies
      - docker-patterns
      - kubernetes-patterns

  - name: code-review-partial
    description: "Code review with performance checks (security auditor skill not available in base registry)"
    type: parallel
    execution: parallel
    skills:
      - performance-optimization
      - testing-strategies

  - name: skill-audit
    description: "Audit a skill's quality: evaluate skill design and judge quality in parallel"
    type: parallel
    execution: parallel
    skills:
      - skill-creator
      - skill-judge

  - name: docs-pipeline
    description: "Documentation creation pipeline: code docs followed by project docs"
    type: chain
    execution: serial
    skills:
      - code-docs
      - project-docs

  - name: git-workflow
    description: "Git workflow: commit message generation followed by release preparation"
    type: chain
    execution: serial
    skills:
      - git-commit
      - git-release

  - name: content-creation
    description: "Content creation with research and writing"
    type: chain
    execution: serial
    skills:
      - content-research-writer
      - humanizer